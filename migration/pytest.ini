[pytest]
testpaths = tests
python_files = test_*.py
markers =
    build: runs the Hugo build (slow); requires the hugo binary
    generated: asserts over generated content/blog (run the migration first)
addopts = -ra
